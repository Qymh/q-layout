// eslint-disable-next-line no-unused-vars
import { CollectionGroup } from '../types';

export const userCollections: CollectionGroup = {
  box: {
    raw: 'boxSizing',
    type: String,
    default: '',
    pure: true
  }
};
