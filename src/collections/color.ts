// eslint-disable-next-line no-unused-vars
import { CollectionGroup } from '../types';

export const colorCollections: CollectionGroup = {
  opacity: {
    raw: 'opacity',
    type: [String, Number],
    default: '',
    pure: true
  }
};
